const num = [1, 2, 3, 4, 5, 6];

for (let i = 0; i < num.length; i++) {
  console.log(num[i]);
}

const output = (i = console.log(i));

num.forEach(output);
forEach(output, num);

const val = true ? "this" : "that";
